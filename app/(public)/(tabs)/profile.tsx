import { PublicLockedScreen } from "@/components/access/PublicLockedScreen";

export default function PublicProfileScreen() {
  return <PublicLockedScreen reason="profile" kicker="YOUR PROFILE LOCKED" />;
}
